services:
  - type: web
    name: my-telegram-bot
    runtime: python
    repo: https://github.com/Mirotvorec87/telegram-bot
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bot:application
